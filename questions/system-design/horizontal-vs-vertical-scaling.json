{
  "question": "What best describes horizontal scaling?",
  "options": [
    "Adding more machines to share the load",
    "Adding more CPU and RAM to a single machine",
    "Rewriting the app in a faster language",
    "Moving the database onto the same host as the app"
  ],
  "correct": 0,
  "explanation": "Horizontal scaling (scaling out) adds more nodes behind a load balancer, improving fault tolerance and near-linear capacity. Vertical scaling (scaling up) grows a single machine and eventually hits a hardware ceiling."
}

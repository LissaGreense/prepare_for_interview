{
  "question": "What does CPython's GIL protect?",
  "options": [
    "Access to Python objects' reference counts",
    "The filesystem from concurrent writes",
    "GPU memory allocation",
    "Network sockets"
  ],
  "correct": 0,
  "explanation": "The GIL serializes access to interpreter state, notably refcounts, so only one thread executes Python bytecode at a time."
}
